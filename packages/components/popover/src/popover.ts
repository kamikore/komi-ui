

export const popoverProps = {
    trigger: {
        type: String,
        default: 'hover'
    },
    showArrow: {
        type: Boolean,
        default: true
    },
    placement: {
        type: String,
        default: 'bottom'
    }
}